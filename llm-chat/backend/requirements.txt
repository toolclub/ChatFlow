# ── Web 框架 ──
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
httpx>=0.27.0

# ── LangChain 生态 ──
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-openai>=0.3.0          # ChatOpenAI（兼容 Ollama/vLLM/OpenAI 等 OpenAI 接口）
langchain-ollama>=0.2.0          # OllamaEmbeddings（Embedding 使用 Ollama 原生 API）
langchain-qdrant>=0.2.0          # QdrantVectorStore

# ── LangGraph（Agent 编排） ──
langgraph>=0.3.0

# ── MCP 支持 ──
langchain-mcp-adapters>=0.1.0

# ── 向量数据库 ──
qdrant-client>=1.9.0

# ── 内置工具依赖 ──
ddgs>=1.0.0                      # web_search 工具（DuckDuckGo，支持中文区域搜索）
